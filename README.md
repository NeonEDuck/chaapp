# Instruction

```bash
$ python -m venv venv

$ source venv/bin/activate

(venv)$ pip install django==3.2

(venv)$ pip install django-simple-captcha

(venv)$ python manage.py createsuperuser

(venv)$ python manage.py migrate
```